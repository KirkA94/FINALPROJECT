export default function CreateUser() {
    return (
      <div style={{ padding: '20px', textAlign: 'center' }}>
        <h1>Create a New User</h1>
        <p>This is where the user creation form will go.</p>
      </div>
    );
  }